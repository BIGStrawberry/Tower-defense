#pragma once

#pragma region comment
/**
* @struct Encapsulates all the (modifiable) data that is needed by Tower.
*/
#pragma endregion

struct TowerData {
	int radius;
	int reload_time;
	int damage;
};